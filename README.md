# SoilDataLoggerr
This project will simulate how IoT system would log and analyze soil data but entirely in Python. It simply takes user input (soil moisture &amp; temperature) ,stores that data in a CSV file  and reads back the data as well as it shows a summary (min, max, avg).
